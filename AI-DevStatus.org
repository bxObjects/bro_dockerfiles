#+title: Development Status & Continuation Handoff
#+TAGS: me ai aiAndMe

# Provenance (installed once by aiActivity.cs --- not regenerated):
#   Date:      2026-08-07T14:22:54-07:00
#   User:      bystar
#   Templates: /bisos/apps/defaults/ai-templates
#   Command:   /bisos/pipx/bin/aiActivity.cs -i initiate --activity=custom --noLink=AI-Activity.org

####+BEGIN: b:ai:file/particulars
* AI Working Context
  Working Directory: /bisos/git/auth/bxRepos/bxObjects/bro_dockerfiles
  File: /bisos/git/auth/bxRepos/bxObjects/bro_dockerfiles/AI-DevStatus.org
  Companion Docs: AI-WORKFLOW.org, AI-Activity.org, AI-DevStatus.org, README.org
###+END:

This file is a working-state handoff so development can continue on another
machine (which has this repo but NOT the developer's =~=). It captures the
architecture, what works, what is untested and the design decisions made so far.

This file is maintained by Claude. Updates are typically triggered by the
=/bx-update-status= slash command, after major interactions, or before a release.

* Current State

As of 2026-08-10. All Stage 1--4 TODOs complete. Two post-Stage-4 fixes applied
and verified: =bystar= home directory permissions (700→755) and =/tmp= noexec
removed via a =tmp-exec.service= systemd oneshot. Both confirmed on RHEL 8.10
(cgroup v1) and on Debian hosts. README.org updated to reflect both fixes.

Stage 5 planting is complete. =dockerProc.spcs= is planted in all four docker
leaves (deb12/13 × confined/privileged) and =podmanProc.spcs= in both rootless-sysd
leaves. All six paths derive correct parameters from their directory location via
=bisos.dockerProc.containerProc_seedInfo.paramsFromPlantPath()=. The per-leaf bash
scripts (=build.bash=, =verify.sh=, =run.bash=, =docker-compose.yml=) remain in place
pending live end-to-end verification of the spcs commands against a running container.

The repo has a three-peer image hierarchy along an *init/isolation profile*
axis: =confined= (docker, unprivileged, no systemd), =privileged= (docker,
=--privileged=, systemd PID 1), and =rootless-sysd= (podman, rootless, systemd
PID 1). Six image specifications across two Debian releases and three profiles.

Verified end-to-end:
- *deb13 privileged sysd on cgroup v2* (prior session, Debian host) — PASS
- *deb13 rootless-sysd on cgroup v2* (prior session, Debian host) — PASS
- *deb13 privileged sysd on cgroup v1* (RHEL 8.10, this session) — PASS (11/11)
- *bystar home 755 + /tmp exec* confirmed in running deb13 privileged container

Still untested: deb12 variants (same pattern, high confidence); rootless-sysd
deb12; Quadlet per-engineer path; confined images after rename.

* Architecture

** Directory hierarchy

#+BEGIN_SRC
debian/
  <majorRelease>/              12 or 13
    confined/vnc/xfce/         unprivileged, entrypoint.sh init
      bisos_deb12-fresh/       (or bisos_deb13-fresh/)
    privileged/vnc/xfce/       --privileged (docker), systemd PID 1
      bisos_deb12-sysd/        (or bisos_deb13-sysd/)
    rootless-sysd/vnc/xfce/    rootless podman, systemd PID 1 (cgroup v2)
      bisos_deb12-rootless-sysd/ (or bisos_deb13-rootless-sysd/)
#+END_SRC

The path structure is not cosmetic --- it is the *configuration substrate* for
the Stage 5 spcs framework. Each path segment encodes a parameter:
=debian/<release>/<profile>/vnc/xfce/<imageName>/= → distro, release, profile
(→ engine/privilege/init), desktop type, image name. The =dockerProc= seed will
parse =plantOfThisSeed= to derive the full operating context with no per-leaf
parameter files.

** Confined images (=bisos_deb{12,13}-fresh=)

- Base: =debian:12.12= / =debian:13.1= built from scratch
- Init: =dockerConf/entrypoint.sh= starts sshd, VNC, noVNC directly; =sleep infinity= keeps container alive
- VNC xstartup: =dockerConf/xstartup= launches =startxfce4=
- User: =bystar= created via =useradd= with =chmod 755 /home/bystar= (useradd default is 700)
- Volume: =./= mounted at =/shuttle/this=; =raw-bisos/= mounted at =$HOME/raw-bisos= at runtime
- Ports (host): SSH=2222, VNC=5901, noVNC=6901

** Privileged images (=bisos_deb{12,13}-sysd=)

- Base: =FROM bisos/deb12-fresh-vnc-xfce:1.21= (or deb13 equivalent) — layers on confined
- =USER root= immediately after FROM (base ends as =USER bystar=; systemd + build steps need root)
- Init: =/sbin/init= (systemd PID 1); =STOPSIGNAL SIGRTMIN+3=
- Services: =vncserver.service=, =novnc.service=, =sshd-container.service=, =tmp-exec.service= installed and enabled
- =tmp-exec.service=: oneshot that runs =mount -o remount,exec /tmp= early in boot;
  needed because Docker's tmpfs mounts apply =noexec= by default
- Suppressed units: udev, getty, plymouth, rescue, initctl
- Masked: =polkit.service= (fails in containers, would leave systemd =degraded=)
- docker-compose.yml (v2 host): =privileged: true=, =cgroup: host=, =/sys/fs/cgroup= rw,
  =/sys/fs/fuse=, tmpfs on =/tmp /run /run/lock /var/log/journal=
- docker-compose.cgv1.yml (v1 host): =privileged: true=, NO =cgroup: host=,
  =/sys/fs/cgroup:ro= + =/sys/fs/cgroup/systemd:rw=, container_name suffixed =-cgv1=;
  includes explicit =networks:= block with =subnet: 10.254.13.0/24= (deb13) to handle
  hosts where Docker's default bridge pool is exhausted
- =build.bash=: supports =-d= (local build) and =-n= (=--no-cache=)
- Ports (host): deb12 SSH=2223 VNC=5902 noVNC=6902; deb13 SSH=2224 VNC=5903 noVNC=6903

** Rootless-sysd images (=bisos_deb{12,13}-rootless-sysd=)

- Build identical to privileged sysd (FROM confined + =USER root=, systemd, unit
  suppression, enabled services, ENTRYPOINT =/sbin/init=). Difference is run-time only.
- Run: =run.bash= → =podman run --systemd=always=, NO =--privileged=, NO
  =/sys/fs/cgroup= mount, NO cgroupns. Podman does cgroup delegation + tmpfs itself.
- Per-engineer multi-tenant form: Quadlet =*.container= unit (=systemctl --user=
  + =loginctl enable-linger=).
- =build.bash=: uses =--isolation=chroot= for all podman builds; auto-builds confined
  base if missing in Podman's store.
- =verify.sh=: exec-free (SSH-based); =degraded= is WARN not FAIL.
- Ports (host): deb12 SSH=2225 VNC=5904 noVNC=6904; deb13 SSH=2226 VNC=5905 noVNC=6905
- Requires: rootless Podman + crun + cgroup v2 with controller delegation.
  Does NOT work on cgroup v1.

** Host cgroup v1/v2 compatibility

| Profile         | cgroup v1 host                    | cgroup v2 host |
|-----------------+-----------------------------------+----------------|
| confined        | works                             | works          |
| privileged sysd | works via docker-compose.cgv1.yml | works (.yml)   |
| rootless-sysd   | does not work                     | works          |

Check with: =stat -fc %T /sys/fs/cgroup= (=cgroup2fs= = v2; =tmpfs= = v1/hybrid).
RHEL 8 defaults to v1; flip via =grubby ... systemd.unified_cgroup_hierarchy=1= + reboot.

** Stage 5: containerProc spcs framework

The per-leaf bash scripts are pre-framework scaffolding. The replacement:

- Docker leaves plant =dockerProc.spcs=; rootless-sysd leaves plant =podmanProc.spcs=
- Both delegate to =containerProc-seed.cs= in =bisos.dockerProc=
- The seed parses =plantOfThisSeed= path to derive all operating parameters —
  no per-leaf configuration files needed
- Leaf directories will contain only =Dockerfile=, =dockerConf/= assets, and the
  thin =.spcs= file

Path-to-parameter mapping:

| Path segment  | Parameter derived                                                      |
|---------------+------------------------------------------------------------------------|
| =debian=      | distro                                                                 |
| =<release>=   | 12 or 13 → base OS, base image tag                                    |
| =<profile>=   | =confined= / =privileged= / =rootless-sysd= → engine, init, privilege |
| =vnc/xfce=    | desktop type → VNC/noVNC ports, xstartup variant                      |
| =<imageName>= | image name → DockerHub name, container name                           |

Implementation is tracked in =bisos.dockerProc= Stage 1:
=/bisos/git/auth/bxRepos/bisos-pip/dockerProc/AI-WorkPlan.org=

** Key files per image

| File                                | Purpose                                      |
|-------------------------------------+----------------------------------------------|
| =Dockerfile=                        | Image definition                             |
| =docker-compose.yml=                | Local run config (docker; v2 host)           |
| =docker-compose.cgv1.yml=           | (privileged) cgroup-v1 host overlay          |
| =build.bash=                        | Multi-arch build + push; =-d= local, =-n= no-cache |
| =run.bash=                          | (rootless-sysd) podman run launcher          |
| =*.container=                       | (rootless-sysd) Podman Quadlet unit          |
| =verify.sh=                         | (sysd) host-side smoke test                  |
| =dockerConf/entrypoint.sh=          | (confined) Manual service startup            |
| =dockerConf/xstartup=               | (confined) VNC → XFCE4 startup              |
| =dockerConf/vncserver.service=      | (sysd) systemd unit for TigerVNC             |
| =dockerConf/novnc.service=          | (sysd) systemd unit for noVNC                |
| =dockerConf/sshd-container.service= | (sysd) systemd unit for sshd                 |
| =dockerConf/tmp-exec.service=       | (privileged) oneshot: remount /tmp exec      |
| =raw-bisos/installRawBisos.sh=      | (confined) Bootstraps Raw-BISOS in container |

* What Works

- *deb13 privileged sysd on cgroup v2 (Debian host): VERIFIED* — prior session.
  systemd PID 1, all three services active, SSH/VNC reachable, =verify.sh= all green.
- *deb13 privileged sysd on cgroup v1 (RHEL 8.10): VERIFIED* — this session.
  All 11 verify.sh checks pass. =/home/bystar= is 755. =/tmp= is exec (no noexec).
- *deb13 rootless-sysd on cgroup v2: VERIFIED* — prior session on Debian host.
- Confined deb12 and deb13 images: complete and consistent.
- Directory hierarchy: three-peer profile axis, all path references correct.
- Stage 5 framework: fully designed and documented in AI-WorkPlan.org.

* What Is Untested

- deb12 privileged sysd: not built/run (deb13 verified on both v1 and v2; deb12
  is identical pattern, high confidence but unconfirmed).
- rootless-sysd deb12: same pattern as deb13 rootless-sysd, unconfirmed.
- rootless-sysd deb13 Quadlet path: =systemctl --user= + linger not exercised.
- Confined images: not re-tested after the =desktop= → =confined/vnc= rename,
  though Dockerfile and scripts are unchanged.
- Stage 5 spcs live commands: =dockerProc.spcs= planted and path-verified in all six leaves;
  actual =build= / =verify= against a running container not yet exercised end-to-end.
  Per-leaf bash scripts remain until live verification passes.

* Key Design Decisions

- *init/isolation profile as the hierarchy dimension* — three peers named by
  property (=confined=, =privileged=, =rootless-sysd=), not by tool (docker/podman).

- *rootless-sysd collapses the confined/privileged tradeoff* — systemd PID 1
  with unprivileged posture, via rootless Podman. Requires cgroup v2.

- *Privileged/rootless images layer on confined* — =FROM bisos/deb{12,13}-fresh-vnc-xfce=
  so package updates flow through on rebuild; no duplication of apt-get installs.

- *VNC/noVNC/sshd as systemd units* — restart-on-failure, dependency ordering,
  =systemctl status= visibility. Replaces manual =entrypoint.sh= approach.

- *bystar home dir 755* — =useradd= default is 700; =chmod 755= added to confined
  Dockerfiles. Privileged/rootless inherit via FROM.

- *tmp-exec.service oneshot* — Docker's =tmpfs:= shorthand applies =noexec= by
  default. A systemd oneshot running =mount -o remount,exec /tmp= at boot corrects
  this to match Debian's default. Simpler alternatives (compose long-form tmpfs,
  tmp.mount.d drop-in) do not work because Docker mounts /tmp before systemd starts.

- *cgroup: host (v2)* — correct Compose key (maps to =--cgroupns=host=); the
  invalid =cgroupns: host= key was used originally and rejected by the schema.

- *cgroup-v1 overlay* — =docker-compose.cgv1.yml= drops =cgroup: host= and uses
  classic v1 recipe (=/sys/fs/cgroup:ro= + =/sys/fs/cgroup/systemd:rw=).

- *Explicit subnet in cgv1 compose* — shared hosts exhaust Docker's default bridge
  pool (172.17--172.31, 192.168/16). Explicit =subnet: 10.254.13.0/24= sidesteps
  "all predefined address pools have been fully subnetted". Adjust per host.

- *build.bash -n for --no-cache* — Docker layer cache can serve stale layers after
  Dockerfile changes if intermediate layers are unchanged. =-n= forces a clean build.

- *USER root in sysd Dockerfiles* — confined base ends as =USER bystar=; privileged
  Dockerfiles need =USER root= right after =FROM= for apt/systemctl build steps.

- *polkit masked* — fails in containers (no seat/active session), leaves systemd
  =degraded=. Masked in all four sysd Dockerfiles.

- *--isolation=chroot for rootless builds (old Podman)* — Podman 4.3.1 buildah
  sd-bus failure workaround. Harmless on newer Podman; kept for portability.

- *verify.sh exec-free (rootless-sysd)* — =podman exec= broken on old Podman for
  systemd containers. Uses host ports + noVNC HTTP + SSH session instead.

- *graphroot on large local disk* — rootless Podman store must not be on near-full
  or NFS filesystem. Set via =~/.config/containers/storage.conf=.

- *Debian 12 vs 13 host (rootless-sysd)* — Podman 4.3.1 (Debian 12) needs chroot
  isolation + exec-free verify. Podman 5.4.2 (Debian 13) fixes both.

- *Directory path as configuration (Stage 5)* — the =debian/<release>/<profile>/vnc/xfce/<imageName>/=
  hierarchy encodes all operating parameters. =dockerProc.spcs= (docker leaves) and
  =podmanProc.spcs= (rootless-sysd leaves) both delegate to =containerProc-seed.cs=,
  which parses =plantOfThisSeed= to derive engine, privilege level, ports, base image, etc.
  No per-leaf parameter files needed. Consistent with the BISOS spcs pattern
  (=pypiProc.sh=, =djangoProc.spcs=). Implemented in =bisos.dockerProc=.

# Local Variables:
# eval: (setq-local toc-org-max-depth 4)
# End:
