#+title: Development Status & Continuation Handoff
#+TAGS: me ai aiAndMe

# Provenance (installed once by aiActivity.cs --- not regenerated):
#   Date:      2026-08-07T14:22:54-07:00
#   User:      bystar
#   Templates: /bisos/apps/defaults/ai-templates
#   Command:   /bisos/pipx/bin/aiActivity.cs -i initiate --activity=custom --noLink=AI-Activity.org

####+BEGIN: b:ai:file/particulars
* AI Working Context
  Working Directory: /bisos/git/auth/bxRepos/bxObjects/bro_dockerfiles
  File: /bisos/git/auth/bxRepos/bxObjects/bro_dockerfiles/AI-DevStatus.org
  Companion Docs: AI-WORKFLOW.org, AI-Activity.org, AI-DevStatus.org, README.org
###+END:

This file is a working-state handoff so development can continue on another
machine (which has this repo but NOT the developer's =~=). It captures the
architecture, what works, what is untested and the design decisions made so far.

This file is maintained by Claude. Updates are typically triggered by the
=/bx-update-status= slash command, after major interactions, or before a release.

Note to a fresh Claude: this file is intentionally sparse at project start.
Fill in the sections below when asked to update, following the structure provided.

* Current State

As of 2026-08-07. All Stage 1 and Stage 2 TODOs completed in this session.

The repo is clean of vagrant relics and has a well-structured two-tier image
hierarchy. Four image specifications exist across two Debian releases and two
privilege levels. README.org accurately describes the current state.

The privileged images are authored but not yet built or tested — they await
execution on a Debian 12/13 host with Docker Engine and cgroupv2.

* Architecture

** Directory hierarchy

#+BEGIN_SRC
debian/
  <majorRelease>/           12 or 13
    confined/vnc/xfce/      unprivileged, entrypoint.sh init
      bisos_deb12-fresh/    (or bisos_deb13-fresh/)
    privileged/vnc/xfce/    --privileged, systemd PID 1
      bisos_deb12-sysd/     (or bisos_deb13-sysd/)
#+END_SRC

** Confined images (=bisos_deb{12,13}-fresh=)

- Base: =debian:12.12= / =debian:13.1= built from scratch
- Init: =dockerConf/entrypoint.sh= starts sshd, VNC, noVNC directly; =sleep infinity= keeps container alive
- VNC xstartup: =dockerConf/xstartup= launches =startxfce4=
- Volume: =./= mounted at =/shuttle/this=; =raw-bisos/= symlinked into =$HOME= at runtime
- Ports (host): SSH=2222, VNC=5901, noVNC=6901

** Privileged images (=bisos_deb{12,13}-sysd=)

- Base: =FROM bisos/deb12-fresh-vnc-xfce:1.21= (or deb13 equivalent) — layers on confined
- Init: =/sbin/init= (systemd PID 1); =STOPSIGNAL SIGRTMIN+3=
- Services: =vncserver.service=, =novnc.service=, =sshd-container.service= installed and enabled
- Suppressed units: udev, getty, plymouth, rescue, initctl — these do not work in containers
- docker-compose: =privileged: true=, =cgroupns: host=, =/sys/fs/cgroup= rw, =/sys/fs/fuse=,
  tmpfs on =/tmp /run /run/lock /var/log/journal=
- Ports (host): deb12 SSH=2223 VNC=5902 noVNC=6902; deb13 SSH=2224 VNC=5903 noVNC=6903
- Requires: Debian 12/13 host, Docker Engine, cgroupv2 (=stat -fc %T /sys/fs/cgroup= → =cgroup2fs=)

** Key files per image

| File                            | Purpose                                      |
|---------------------------------+----------------------------------------------|
| =Dockerfile=                    | Image definition                             |
| =docker-compose.yml=            | Local run configuration                      |
| =build.bash=                    | Multi-arch build + DockerHub push            |
| =dockerConf/entrypoint.sh=      | (confined) Manual service startup            |
| =dockerConf/xstartup=           | (confined) VNC → XFCE4 startup              |
| =dockerConf/vncserver.service=  | (privileged) systemd unit for TigerVNC       |
| =dockerConf/novnc.service=      | (privileged) systemd unit for noVNC          |
| =dockerConf/sshd-container.service= | (privileged) systemd unit for sshd       |
| =raw-bisos/installRawBisos.sh=  | (confined) Bootstraps Raw-BISOS in container |

* What Works

- Confined deb12 and deb13 images: Dockerfile, compose, build.bash, all
  dockerConf files are complete and consistent with the upstream working base.
- Directory hierarchy: clean, no vagrant relics, all path references updated.
- README.org: accurate and complete for the current structure.
- Privileged image files: authored and structurally complete.

* What Is Untested

- Privileged images have not been built or run yet. The following need
  verification on a Debian 12/13 host:
  - =docker build= succeeds (=systemctl enable= at build time without running systemd)
  - systemd boots cleanly inside the container (no hung units)
  - =vncserver.service=, =novnc.service=, =sshd-container.service= start and stay up
  - Socket activation works for user-added systemd units
  - =journalctl= and =systemctl= behave as expected
- Confined images: not re-tested after the =desktop= → =confined/vnc= rename,
  though the Dockerfile and scripts are unchanged.

* Key Design Decisions

- *confined/privileged as hierarchy dimension* — privilege level is a first-class
  concern that affects init, cgroup config, and security posture. Making it an
  explicit directory level (rather than a name suffix) keeps future variants clean.

- *Privileged images layer on confined* — =FROM bisos/deb12-fresh-vnc-xfce= means
  package updates to the confined image flow through to privileged automatically
  on rebuild. No duplication of the full apt-get install list.

- *VNC/noVNC/sshd as systemd units in privileged* — replaces the manual
  =entrypoint.sh= approach. Gives restart-on-failure, proper dependency ordering
  (noVNC after VNC), and =systemctl status= visibility.

- *udev suppressed* — =systemd-udevd= is removed from the privileged image because
  there is no real hardware to manage inside a container. All other systemd
  functionality is preserved.

- *cgroupns: host* — required for systemd to correctly manage service scopes.
  Without it, systemd sees a private cgroup namespace and some socket/scope
  operations fail.

# Local Variables:
# eval: (setq-local toc-org-max-depth 4)
# End:
