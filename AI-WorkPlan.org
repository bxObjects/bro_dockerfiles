#+title: Work Plan
#+DATE: <2026-06-10 Wed 13:54>
#+AUTHOR: Mohsen BANAN
#+OPTIONS: toc:4
#+TAGS: me ai aiAndMe

# Provenance (installed once by aiActivity.cs --- not regenerated):
#   Date:      2026-08-07T14:22:54-07:00
#   User:      bystar
#   Templates: /bisos/apps/defaults/ai-templates
#   Command:   /bisos/pipx/bin/aiActivity.cs -i initiate --activity=custom --noLink=AI-Activity.org

####+BEGIN: b:ai:file/particulars
* AI Working Context
  Working Directory: /bisos/git/auth/bxRepos/bxObjects/bro_dockerfiles
  File: /bisos/git/auth/bxRepos/bxObjects/bro_dockerfiles/AI-WorkPlan.org
  Companion Docs: AI-WORKFLOW.org, AI-Activity.org, AI-DevStatus.org, README.org
###+END:

This Work Plan file drives the AI collaborative development.
Interactions are broken down into "Stages". Each Stage has a set
of TODOs that I work with Claude to accomplish.

Note to a fresh Claude: read this file together with AI-WORKFLOW.org,
AI-Activity.org and AI-DevStatus.org to orient yourself. The =<<Context Confirmation>>=
TODO below is the standard first step, or use =/bx-confirm0= as the automated equivalent.
TODOs tagged =:ai:= are assigned to Claude. After completing a TODO, summarize results
under =----- AI Results: ------= and mark it DONE.

* Related AI-Activities

@/bisos/git/bxRepos/bisos-pip/dockerProc/CLAUDE.md

Related Blee panel (permanent, human-oriented Docker/container docs and
cross-cutting decisions):
=/bisos/git/auth/bxRepos/blee-binders/bisos-core/virtualization/docker/_nodeBase_/fullUsagePanel-en.org=
(also at =/bisos/panels/bisos-core/virtualization/docker/=). E.g. it records the
=/dev/shm=2G= resize (=dev-shm-resize.service=) on the privileged sysd leaves for Chrome.

* Stage 0: Confirm AI's Understanding of This Repo's Context

Goal: Confirm AI's understanding of the context and capture that in [[Context Confirmation]] TODO.


* Stage 0 TODOs
** +
** DONE [#A] <<Context Confirmation>> for AI (typically triggered by /bx-confirm0) :aiAndMe:
SCHEDULED: <2026-06-09 Tue>
CLOSED: [2026-09-03 Thu]
Must be repeated each time a new Claude instance is started here.
Based on the series of files starting from CLAUDE.md in this repo,
describe succinctly what you have understood.
Enumerate explicitly which files you have imported and which you have considered.
Prompt the user to confirm that the desired/appropriate model (/model) is being used for this project.
----- AI Results: ------
(Record refreshed [2026-09-03 Thu]; superseded the 2026-08-07 record, two of
whose claims had gone stale --- see the last paragraph.)

Local chain (in order): CLAUDE.md (regular file) → AI-WORKFLOW.org
(symlink → /bisos/apps/defaults/ai-templates/mother/AI-WORKFLOW.org),
AI-Activity.org (safe-copied =custom= template, *placeholders still
unfilled*), AI-DevStatus.org, AI-WorkPlan.org.

Cross-repo chain, via =@/bisos/git/bxRepos/bisos-pip/dockerProc/CLAUDE.md= in
this file: *now resolves* (=/bisos/git/bxRepos= → =/bisos/git/auth/bxRepos=).
It pulls in dockerProc's CLAUDE.md, its AI-WORKFLOW.org (symlink, same
=mother/= file), AI-Activity.org (symlink → =ai-templates/bisos-pip/= ---
shared and fully filled: COMEEGA, skills table, conventions), and
dockerProc's own AI-DevStatus.org + AI-WorkPlan.org. Through that, the
bisos-pip reference panels (bisos.b, PyCsFwrk, pycsFwrkLayerings,
pyAdoptedPkgs, csPlayers, bisos-pip, bisos-pip-packaging) plus
comeega/README.org, poly-dblock/README.org and bxGenesis/start/README.org.

Considered, verified present, but referenced by path only (not auto-imported):
the Docker Blee panel at
=blee-binders/bisos-core/virtualization/docker/_nodeBase_/fullUsagePanel-en.org=.

Repo understood as: BISOS Docker/Podman image specifications --- XFCE4 desktops
over VNC/noVNC/SSH on Debian 12 and 13, on a three-peer *init/isolation
profile* axis =debian/{12,13}/{confined,privileged,rootless-sysd}/vnc/xfce/=.
The leaf path IS the configuration substrate: =paramsFromPlantPath()= in
=bisos.dockerProc= derives engine/privilege/ports/base-image from it, so each
leaf carries only a thin =.spcs=. Stages 1--5 planting DONE; one open TODO
here (Stage 5: bring the per-leaf bash scripts to parity) plus a deferred [#C] hostFacts
TODO in Stage 3.

Two discrepancies found this session and worth carrying forward:
- =ftoBranchProc.spcs= exists at every branch level (9 of them) and is
  documented nowhere in AI-DevStatus.org or this file.
- =bisos.dockerProc/AI-DevStatus.org= still advertises the *pre-rename*
  Cmnd names (=containerProc_build=, =containerProc_composeUp=); that repo's
  README.org has the new noun-consistent names. Stale in the handoff doc only.

Model: claude-opus-5[1m] (Opus 5, 1M context). Pending user confirmation.
** -

* Stage 1: Rename desktop to vnc and cleanup

Goal: Make sure that this having been copied from vagrant, is now clean and rename desktop to vnc


* Stage 1 TODOs
** +
** Archived. No longer relevant.
** -

* Stage 2: Add confined/privileged hierarchy and privileged systemd containers

Goal: Restructure the directory hierarchy to make privilege level explicit, and
create privileged container variants with systemd as PID 1.


* Stage 2 TODOs
** +
** Archived. No longer relevant.
** -

* Stage 3: Add rootless-sysd (Podman) variant

Goal: Add a third peer to the confined/privileged init-profile axis: a
rootless Podman systemd container that delivers the =privileged= payoff
(systemd as PID 1) with the =confined= security posture (unprivileged,
userspace). Motivated by the multi-tenant "N engineers on one shared VM"
use case, where Docker =--privileged= is a security dealbreaker but engineers
still need systemd fidelity inside their dev box.

Note on the model: the directory level formerly read as "privilege level"
(confined vs privileged) is now better understood as an "init/isolation
profile" axis. The three peers are named by property, not by tool:
=confined= (docker, unprivileged, no systemd), =privileged= (docker,
--privileged, systemd), =rootless-sysd= (podman, unprivileged, systemd).


* Stage 3 TODOs
** +
** TODO [#C] hostFacts: examples menu also aware of host identity/capability :aiAndMe:
SCHEDULED: <2026-08-11 Tue>
_Code owner: =bisos.dockerProc/py3/bisos/dockerProc/= (new module =hostFacts.py=)_

Depends on the rename + examples-filtering TODOs above. Deferred until
Stage 4 registrar work is underway --- both need per-host state.

The examples menu should further filter based on *host* attributes,
not just leaf attributes. E.g. on a cgroup-v1 host, always suggest
=--cgroupVer="v1"= as the default; on a cgroup-v2 host, hide it. On
=c1= (Debian 12), never suggest =rootless-sysd= (Podman 4.3.1 has
known bugs); on =c2= (RHEL 8), warn about cgroup v1 + no reboot.

Introduce a new pure module =containerProc/hostFacts.py=:
- =HostFacts= dataclass: =hostname=, =cgroupVer=, =dockerAvailable=,
  =podmanAvailable=, =podmanVersion=, =debianRelease= (if applicable).
- =probeHostFacts() -> HostFacts= — pure probe function, no side effects.

Feed =HostFacts= into the same context-specific examples generator
built in the previous TODO, alongside =p= from =paramsFromPlantPath()=.

Naturally couples to the Stage 4 Platform Registrar (dockerProc Stage
4 TODO): the registrar's per-user, per-image, per-instance file tree
grows a sibling per-host branch at
=/bisos/platform/dockerProc/hostFacts/<hostname>/fps/=.
----- AI Results: ------

** -

* Stage 4: Host cgroup constraints and the cgroup-v1 privileged overlay

Goal: Capture which of the four models actually work on a given host as a
function of its cgroup version and privilege policy, and provide a working
systemd option for old (cgroup v1) hosts that cannot be rebooted. Driven by a
concrete situation: a corporate AWS *RHEL 8.10* host, still on cgroup v1, that
cannot be rebooted.

** Background: cgroup v1 vs v2 decision matrix

The init/isolation profiles depend on the HOST cgroup version. You cannot
switch v1 <-> v2 at runtime --- the unified hierarchy is chosen at boot.
Check with: =stat -fc %T /sys/fs/cgroup= (=cgroup2fs= = v2; =tmpfs= = v1/hybrid).

| Model                     | cgroup v1 host                         | cgroup v2 host |
|---------------------------+----------------------------------------+----------------|
| confined (docker/podman)  | works (cgroup-agnostic)                | works          |
| privileged sysd (docker)  | works, but needs the v1 compose overlay| works (v2 yml) |
| rootless-sysd (podman)    | DOES NOT WORK (no v1 rootless deleg.)  | works          |
| Vagrant VM                | works IF /dev/kvm present              | same           |

Key facts established:
- *rootless-sysd requires cgroup v2.* cgroup v1 has no safe delegation model,
  so an unprivileged user cannot get a writable cgroup subtree; rootless
  systemd-as-PID-1 and rootless cpu/mem limits simply do not work on v1.
  RHEL 8 hybrid mounts a v2 node at =/sys/fs/cgroup/unified= but it carries no
  controllers, so this does not rescue rootless delegation.
- *privileged sysd works on v1* via the classic recipe: =--privileged=, mount
  =/sys/fs/cgroup:ro= + =/sys/fs/cgroup/systemd:rw=, tmpfs on =/run /run/lock=,
  and NO =cgroup: host= (cgroup namespaces are a newer-kernel feature).
- *Vagrant on AWS* needs nested virtualization (=/dev/kvm=), available only on
  =.metal= instance types --- orthogonal to the cgroup question.

** RHEL 8.10 specifics (the work host)

- RHEL 8 (kernel 4.18) *supports* cgroup v2 but *defaults to v1/hybrid*.
- The supported flip is a boot-time kernel arg + reboot:
  =sudo grubby --update-kernel=ALL --args="systemd.unified_cgroup_hierarchy=1"=
  then reboot; then add a user delegation drop-in
  (=/etc/systemd/system/user@.service.d/delegate.conf= with
  =Delegate=cpu cpuset io memory pids=).
- *Constraint: the host cannot be rebooted.* Therefore it stays on cgroup v1
  and *rootless-sysd is unavailable there*. No runtime/userspace workaround
  exists.
- No-reboot options on that host: =confined= always; =privileged= sysd via the
  v1 overlay *iff* =--privileged= is permitted.
- AWS-native escape hatch that avoids rebooting the pet: stand up a *new*
  instance already on cgroup v2 (RHEL 9 / Amazon Linux 2023 default to v2, or a
  RHEL 8 launch template with the kernel arg baked into user-data), and a
  =.metal= type if the Vagrant/true-VM path is also wanted.

** Determining whether =--privileged= is allowed

"I have root" answers a different question than "am I authorized." Two levels:
- *Technical (can the daemon do it):* check for an authorization plugin
  (=docker info --format '{{.Plugins.Authorization}}'=), userns-remap/rootless
  mode (=docker info | grep -iE 'userns|rootless'=), then empirically test ---
  =docker run --rm --privileged busybox sh -c 'mount -t tmpfs none /mnt && echo PRIV_OK'=.
  =PRIV_OK= = permitted; "authorization denied by plugin" = forbidden.
- *Policy (are you sanctioned):* look for a runtime-security agent
  (falco/twistlock/prisma/aqua/sysdig/crowdstrike) that may allow-but-flag
  privileged containers, and confirm with the security/platform team. On an
  audited corporate host, "root can" != "security permits".


* Stage 4 TODOs
** +
** Archived. No longer relevant. (Reference subsections above are retained.)
** -


* Stage 5: Two peer paths --- standalone bash and =.spcs= (bisos.dockerProc)

Goal: Maintain *both* per-leaf paths as permanent peers, at parity with each
other.

- The *=.spcs= path* --- =dockerProc.spcs= / =podmanProc.spcs= planted in each
  leaf, backed by =containerProc-seed.cs= in =bisos.dockerProc=. Framework
  driven, path-derived parameters, the everyday path *where BISOS is
  installed*.
- The *standalone bash path* --- the per-leaf =build.bash= / =run.bash= /
  =verify.sh= / =docker-compose*.yml=. Self-contained: no BISOS, no pip
  install, nothing but docker/podman required.

*The bash path is not redundant and is not going away.* It is what works on
hosts that do not have BISOS --- concretely the *old RedHat VMs*, where
=pipx install bisos.dockerProc= is not available and the =.spcs= path
therefore cannot run at all. Those hosts still need to build and run these
images. Keeping =bro_dockerfiles= usable on its own is a requirement, not a
transitional convenience.

Implementation of the =.spcs= side is tracked in the =bisos.dockerProc= repo:
=@/bisos/git/auth/bxRepos/bisos-pip/dockerProc/AI-WorkPlan.org=

** Peer equivalence --- bash path and =.spcs= path

Neither column replaces the other. Each row is the *same operation* expressed
in the two paths; parity between the columns is the thing to preserve.

| Standalone bash (no BISOS)  | =.spcs= (requires =bisos.dockerProc=)                      |
|-----------------------------+------------------------------------------------------------|
| =build.bash=                | =containerProc_imageBuild=                                 |
| =docker-compose.yml=        | =containerProc_instanceUp= / =containerProc_instanceDown=  |
| =docker-compose.cgv1.yml=   | =containerProc_instanceUp --cgroupVer=v1=                  |
| =run.bash= (rootless-sysd)  | =containerProc_instanceUp= (=podman run --systemd=always=) |
| =verify.sh=                 | =containerProc_instanceVerify=                             |

=dockerConf/= assets (Dockerfiles, systemd units, entrypoint.sh, xstartup)
remain in each leaf --- they are image-specific content, not logic, and are
shared by both paths.

* Stage 5 TODOs
** +
** DONE [#A] Cleanups of README.org etc based on permanence of build.bash :aiAndMe:
CLOSED: [2026-09-03 Thu 14:46] SCHEDULED: <2026-09-03 Thu>
----- AI Results: ------
README.org, AI-DevStatus.org and the
  =figures/broDockerfiles-graphviz.pcs= figure (plus its regenerated
  =repoStructureAtAGlance.png=) now describe the bash path as the standalone,
  no-BISOS peer of the =.spcs= path.

** TODO [#B] <<BashPathParity>> Update the 8 standalone bash scripts :aiAndMe:
SCHEDULED:  <2026-09-03 Thu 14:49>
The per-leaf bash scripts are *required*, and are a permanent peer of the
=.spcs= path. The =.spcs= path needs =bisos.dockerProc= installed; on the *old
RedHat VMs* BISOS cannot be installed at all, so these self-contained scripts
are the only way to build and run these images there. They are therefore
maintained at parity with the =.spcs= path --- not redundant with it, and not
superseded by it.

The 8 files in question:

: ./debian/13/rootless-sysd/vnc/xfce/bisos_deb13-rootless-sysd/build.bash
: ./debian/13/privileged/vnc/xfce/bisos_deb13-sysd/build.bash
: ./debian/13/confined/vnc/xfce/bisos_deb13-fresh/build.bash
: ./debian/12/rootless-sysd/vnc/xfce/bisos_deb12-rootless-sysd/build.bash
: ./debian/12/privileged/vnc/xfce/bisos_deb12-sysd/build.bash
: ./debian/12/confined/vnc/xfce/bisos_deb12-fresh/build.bash
: ./debian/13/rootless-sysd/vnc/xfce/bisos_deb13-rootless-sysd/run.bash
: ./debian/12/rootless-sysd/vnc/xfce/bisos_deb12-rootless-sysd/run.bash

(=verify.sh=, =docker-compose.yml= and =docker-compose.cgv1.yml= belong to the
same standalone path and are maintained on the same basis.)

Work to do:
- Bring these 8 up to date so the bash path stays at parity with the =.spcs=
  path --- same image names and tags, same host ports, same build flags
  (=-d= local, =-n= =--no-cache=, =--isolation=chroot= for rootless podman),
  same run posture (=--privileged= + cgroup mounts for privileged;
  =--systemd=always= for rootless-sysd).
- For all build.bash make -d default.  Add -u for upload.
- The cp in ./debian/13/confined/vnc/xfce/bisos_deb13-fresh/build.bash should also happen in ./debain/12
- The image names in the bash files should be same as those in .spcs
----- AI Results: ------
Edits done [2026-09-03 Thu]; *not yet run against a docker/podman daemon*
(authored inside a container --- see the =:me:= verification TODO below).

*Decisions taken (user):* (a) dual naming --- build.bash always tags the
=.spcs= name locally, and =-u= additionally tags+pushes the DockerHub name;
(b) =cp -r= is the canonical raw-bisos mechanism, not a symlink.
*Constraint (user):* the old RedHat VMs cannot run podman, so the two
rootless-sysd leaves are off the critical path. The four *docker* leaves
(deb12/13 × confined/privileged) are what the standalone path is for.

*The bash path was broken end-to-end before this.* The Dockerfiles and compose
files had already migrated to =.spcs= naming (=bisos_deb13-fresh:latest=) while
build.bash still tagged =bisos/deb13-fresh-vnc-xfce:4=. So confined build.bash
produced a name nothing referenced, and the sysd leaves' =FROM= could never
resolve. Fixing the names is what makes the path work --- it was not cosmetic.

Per-file changes (all 8):
- *Flags, all 6 build.bash*: local build is now the DEFAULT; =-u= uploads;
  =-n= is =--no-cache= (was privileged-only, now on all six for parity);
  =-d= still accepted as a no-op so existing docs/muscle memory keep working.
  Replaced the broken one-shot =getopts 'd' opt= idiom (which errored on unset
  =$opt=) with a =while getopts 'und'= loop. Parsing verified for
  ='' / -n / -u / '-u -n' / -d=.
- *Names, all 8*: =LOCAL_IMAGE:LOCAL_TAG= now equals =paramsFromPlantPath()='s
  =imageName:latest= in every leaf; DockerHub vars kept but used only by =-u=.
  Both =run.bash= =IMAGE= likewise. Verified bash == Dockerfile =FROM= ==
  compose =image:= across all six leaves.
- *raw-bisos*: the =cp= block added to deb12 confined (it was missing, so that
  leaf could not build at all). Made *idempotent* in both confined leaves and
  in the rootless base-build block --- plain =cp -r <dir> .= nests into
  =./raw-bisos/raw-bisos= on a repeat build, a latent bug in the original
  deb13 script; now =rm -rf ./raw-bisos= then =cp -r <src> ./raw-bisos=.
  Dropped the now-meaningless "NOT rm -r -f" trailer.
- *Base-image guard*: both privileged build.bash now fail fast with the exact
  =( cd <confined leaf> && ./build.bash )= command when the =FROM= base is
  absent, instead of a raw docker error.
- *Dockerfile comments*: both confined Dockerfiles said the =raw-bisos= context
  entry "is a symlink"; it is a =cp=. Corrected.
- All 8 pass =bash -n=.

*Blocker found, NOT fixed here (belongs to bisos.dockerProc):* the =.spcs=
path does no raw-bisos copy at all, and the symlink its Dockerfiles used to
assume is absent and untracked in git. So =containerProc_imageBuild= cannot
build either confined leaf today. =containerProc_imageBuild= needs the same
pre-build copy step. Until then the bash path is the *only* working way to
build the confined images --- on any host, not just RedHat.

** TODO [#B]  Verify them on an old RedHat VM (no BISOS) :me:
SCHEDULED:<2026-09-03 Thu 14:49>
Verify them on an old RedHat VM (no BISOS), which is their actual target
  environment --- not just on a Debian BISOS host, where the =.spcs= path is
  also available and can mask a broken bash path.
----- AI Results: ------


** -


# Local Variables:
# eval: (setq-local toc-org-max-depth 4)
# eval: (setq-local org-log-done 'time)
# End:
